import {styled} from "@mui/material";
import { LoadingButton } from '@mui/lab';
export const RegisterButton = styled(LoadingButton)(({ theme }) => ({
  background: 'rgb(229, 9, 20)',
  height: 50,
  color: 'white',
}));
